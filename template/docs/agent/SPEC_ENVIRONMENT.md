# Environment

This Copier-managed specification defines the generic environment-policy boundary.

Keep secrets out of repository files. Use environment variables or the platform secret store.

Record project-specific environment, build, generated-file, hosting, and CI assumptions in `docs/agent/PROJECT_ENVIRONMENT.md`.
