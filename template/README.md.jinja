# [[ project_name ]]

[[ project_purpose ]]

## Agent Workflow

This repository was initialized from `project-agent-workflow`.

- Start with `AGENTS.md`.
- Use `docs/agent/spec-index.yaml` for task-scoped document routing.
- Track non-trivial work in `docs/plan/plan.md`.
- Keep project-specific rules in `docs/agent/SPEC_*.md`.

## Updating From The Template

Keep `.copier-answers.yml` committed. To update from a tagged template version:

```sh
copier update
```

Review any `*.rej` files manually before committing.

