name: CI

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Copier
        run: python -m pip install --upgrade "copier>=9,<10"

      - name: Lint package structure
        run: scripts/lint-project-workflow.sh

      - name: Smoke test generated projects
        run: tests/smoke.sh
        env:
          REQUIRE_COPIER: "1"

      - name: Test generated hooks
        run: tests/test-hooks.py

      - name: Test Copier update flow
        run: tests/copier-update.sh
        env:
          REQUIRE_COPIER: "1"

      - name: Check whitespace
        run: git diff --check
