interface:
  display_name: "MCP Operations"
  short_description: "Bind provider authentication to each exact call"
  default_prompt: "Use $mcp-ops to authorize one exact identity read without presuming authentication, then authorize the intended provider call."
