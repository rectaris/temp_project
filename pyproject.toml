[project]
name = "project-agent-workflow"
version = "0.0.0"
description = "Reusable coding-agent project workflow template."
requires-python = ">=3.11"
dependencies = [
    "copier>=9.6.0",
    "PyYAML==6.0.3",
]

[tool.uv]
package = false
